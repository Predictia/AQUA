#This include the option for the regridding
target:
  resolution: r100
  frequency: monthly
  outdir: /work/bb1153/b382076/LRA-cycle2
  tmpdir: /scratch/b/b382076
  aggregation: 'M'

configdir: '../../config/'
loglevel: INFO

# all the variable listed here will be processed by the LRA creator
catalog:
  IFS:
    tco3999-ng5:
      ICMGG_atm2d:
        vars: ['2t', 'erate', 'tprate']
    tco2559-ng5:
      ICMGG_atm2d:
        vars: ['ci', 'tprate', '2t', 'erate', 'mslhfl', 'msshfl', 'msnsrf', 'msntrf', 'mtntrf', 'mtnsrf', 'nsss', 'ewss', 'msl', 'hcc', 'lcc', 'mcc']
      ICMU_atm3d:
        vars: ['t', 'u', 'v', 'q']
    tco1279-orca025:
      ICMGG_atm2d:
        vars: ['ci', 'tprate', '2t', 'erate', 'mslhfl', 'msshfl', 'msnsrf', 'msntrf', 'mtntrf', 'mtnsrf', 'nsss', 'ewss', 'msl', 'hcc', 'lcc', 'mcc']
      ICMU_atm3d:
        vars: ['t', 'u', 'v', 'q']

    # tco1279-orca025-cycle3:
    #   2D_monthly_0.25deg:
    #     vars: ['ci', 'sst', 'tprate', '2t', 'mslhfl', 'msshfl', 'msnsrf', 'msntrf', 
    #     'mtntrf', 'mtnsrf', 'nsss', 'ewss', 'msl', 'tcc', 'lcc', 'mcc', 'hcc']
    #   3D_monthly_0.25deg:
    #     vars: ['t', 'u', 'v', 'q']
    # tco2559-ng5-cycle3:
    #   2D_monthly_0.25deg:
    #     vars: ['ci', 'sst', 'tprate', '2t', 'mslhfl', 'msshfl', 'msnsrf', 'msntrf', 
    #     'mtntrf', 'mtnsrf', 'nsss', 'ewss', 'msl', 'tcc', 'hcc', 'lcc', 'mcc']
    #   3D_monthly_0.25deg:
    #     vars: ['t', 'u', 'v', 'q']


