# Template according to #1613
# The first model in this block can be overridden
# in the command line by using:
# --model, --exp, --source, --outputdir, --catalog
datasets:
  - catalog: null
    model: null
    exp: null
    source: 'lra-r100-monthly'
    regrid: null
  # Possible second/third/fourth datasets here, no present by default
  # - catalog: 'obs'
  #   model: 'ERA5'
  #   exp: 'era5'
  #   source: 'monthly'
  #   regrid: r100

references:
  - catalog: 'obs'
    model: 'ERA5'
    exp: 'era5'
    source: 'monthly'
    regrid: null

setup:
  loglevel: 'WARNING'

output:
  outputdir: "./"
  rebuild: true
  save_pdf: true
  save_png: true
  dpi: 300

diagnostics:
  timeseries:
    run: true
    variables: ['2t', 'ie', 'msl',
                'tcc', 'lcc', 'mcc', 'hcc',
                'slhtf', #Mean surface latent heat flux
                'ishf', #Mean surface sensible heat flux
                'sdswrf', #Mean surface downward short-wave radiation flux
                'sdlwrf', #Mean surface downward long-wave radiation flux
                'snswrf', #Mean surface net short-wave radiation flux
                'snlwrf', #Mean surface net long-wave radiation flux
                'tnlwrf', #Mean top net long-wave radiation flux
                'tnswrf' #Mean top net short-wave radiation flux
                ]
    formulae: ['tnlwrf+tnswrf', '86400*tprate']
    params:
      default:
        ref: {catalog: 'obs', model: 'ERA5', exp: 'era5', source: 'monthly'}
        hourly: False
        daily: False
        monthly: True
        annual: True
        std_startdate: '1990-01-01'
        std_enddate: '2020-12-31'
        regions: null
      tnlwrf+tnswrf:
        longname: "Net top radiation"
      86400*tprate:
        longname: "Precipitation"
        units: "mm/day"
        regions: ['tropics', 'europe']

# gregory:
#   ts: '2t'
#   toa: ['tnlwrf', 'tnswrf']
#   monthly: True
#   annual: True