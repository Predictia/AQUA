models:  
    PSC:
        GIOMAS:
            default:
                data_model: false  # Do not attempt to fix coordinates
                delete: [latitude, longitude]
                vars:
                    var262000:  # sithick - sea ice thickness
                        source: sivol
                        grib: true
                    sftof:
                        source: sftof
                        src_units: frac
                        units: frac
        PIOMAS:
            default:
                data_model: false  # Do not attempt to fix coordinates
                delete: [latitude, longitude]
                vars:
                    var262000:  # sithick - sea ice thickness
                        source: sivol
                        grib: true
                    sftof:
                        source: sftof
                        src_units: frac
                        units: frac
