fixer_name:  
  PHC3-default:
    data_model: false
    vars:
      avg_thetao:
        src_units: C
        source: temp
        grib: true
      avg_so:
        src_units: psu
        source: salt
        grib: true
        units: psu