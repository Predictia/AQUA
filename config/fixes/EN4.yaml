fixer_name:  
  EN4-default:
    data_model: false
    vars:
      avg_thetao:
        source: thetao 
        grib: true
      avg_so:
        src_units: psu
        source: so
        grib: true
        units: psu