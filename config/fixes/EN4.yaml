fixer_name:  
  EN4-default:
    data_model: cf
    vars:
      ocpt:
        source: thetao 
        grib: true
      so:
        src_units: psu
        source: so
        grib: true
        units: psu