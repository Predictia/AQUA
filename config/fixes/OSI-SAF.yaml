models:  
    OSI-SAF:
        osi-450:
            default:
                data_model: false  # Do not attempt to fix coordinates
                vars:
                    # Automatic conversion to destination grib units
                    avg_siconc:
                        source: ice_conc
                        grib: true
                    # var262001:  # siconc
                    #     source: ice_conc
                    #     grib: true
