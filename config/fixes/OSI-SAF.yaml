fixer_name:  
    OSI-SAF-default:
        osi-450:
            data_model: false  # Do not attempt to fix coordinates
            vars:
                # Automatic conversion to destination grib units
                var262001:  # siconc
                    source: ice_conc
                    grib: true
