models:  
    GPM:
        imerg:
            default:
                data_model: cf
                vars:
                    # Automatic conversion to destination grib units
                    tprate:
                        source: precipitationCal
                        grib: true