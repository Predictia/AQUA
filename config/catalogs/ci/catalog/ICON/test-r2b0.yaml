plugins:
  source:
  - module: intake_xarray
sources:
  short:
    metadata:
      source_grid_name: test-r2b0
    args:
      chunks:
        time: 1
      urlpath:
      - '{{ TEST_PATH }}/ICON/test-lowres/r2b0.nc'
    description: ICON r2b0 resolution
    driver: netcdf