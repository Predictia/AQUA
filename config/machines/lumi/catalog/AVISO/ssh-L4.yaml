plugins:
  source:
    - module: intake_xarray

sources:
  daily:
    description: AVISO ssh vDT2021    
    driver: zarr
    metadata:
      source_grid_name: aviso
    args:
      consolidated: False
      compat: override
      combine: nested
      urlpath: 
      - reference::/pfs/lustrep3/projappl/project_465000454/data/AQUA/datasets/AVISO/json/AVISO.json