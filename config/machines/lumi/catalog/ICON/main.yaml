sources:
  historical-1990-dev:
    description: FDB ICON historical 1990 run (converted from unreadable FDB)
    driver: yaml_file_cat
    args:
      path: '{{CATALOG_DIR}}/historical-1990-dev.yaml'
  historical-1990:
    #description: FDB ICON historical 1990 run (converted from unreadable FDB)
    driver: yaml_file_cat
    args:
      path: '{{CATALOG_DIR}}/historical-1990.yaml'
  ssp370:
    #description: FDB ICON historical 1990 run (converted from unreadable FDB)
    driver: yaml_file_cat
    args:
      path: '{{CATALOG_DIR}}/ssp370.yaml'
