sources:
  era5:
    description: AQUA compact version of ERA5 data
    driver: yaml_file_cat
    args:
        path: "{{CATALOG_DIR}}/era5.yaml"
  era5-nextgems:
    description: AQUA compact version of ERA5 data with nextgems fixes
    driver: yaml_file_cat
    args:
        path: "{{CATALOG_DIR}}/era5-nextgems.yaml"
  fdb:
    description: FDB version of ERA5 data
    driver: yaml_file_cat
    args:
        path: "{{CATALOG_DIR}}/fdb.yaml"