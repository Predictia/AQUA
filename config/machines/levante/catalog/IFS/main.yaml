sources:
  tco3999-ng5:
    description: 2.8km experiment, coupled with FESOM
    driver: yaml_file_cat
    args:
        path: "{{CATALOG_DIR}}/tco3999-ng5.yaml"
  tco2559-ng5:
    description: 4km experiment, coupled with FESOM
    driver: yaml_file_cat
    args:
        path: "{{CATALOG_DIR}}/tco2559-ng5.yaml"
  tco1279-orca025:
    description: 9km baseline, coupled to NEMO, deep conv ON
    driver: yaml_file_cat
    args:
        path: "{{CATALOG_DIR}}/tco1279-orca025.yaml"
  test-tco2559:
    description: 4km experiment, coupled with FESOM
    driver: yaml_file_cat
    args:
        path: "{{CATALOG_DIR}}/test-tco2559.yaml"
  test-tco79:
    description: TCO79 test data
    driver: yaml_file_cat
    args:
        path: "{{CATALOG_DIR}}/test-tco79.yaml"
  tco2559-ng5-cycle3:
    description: main 4.4km Cycle 3 experiment, coupled with FESOM 5km
    driver: yaml_file_cat
    args:
        path: "{{CATALOG_DIR}}/tco2559-ng5-cycle3.yaml"
  tco1279-orca025-cycle3:
    description: 9km Cycle 3 experiment, coupled with NEMO 25km
    driver: yaml_file_cat
    args:
        path: "{{CATALOG_DIR}}/tco1279-orca025-cycle3.yaml"
