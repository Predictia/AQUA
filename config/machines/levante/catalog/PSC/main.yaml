sources:
  GIOMAS:
    description: Polar Science Center sea ice volume (sivol) for the Southern Hemisphere 
    driver: yaml_file_cat
    args:
      path: "{{CATALOG_DIR}}/giomas.yaml"
  PIOMAS:
    description: Polar Science Center sea ice volume (sivol) for the Northern Hemisphere 
    driver: yaml_file_cat
    args:
      path: "{{CATALOG_DIR}}/piomas.yaml"
