# The top level keys can be anything

detect: 
    template: detect_nodes.sh.j2
    target_volume: output
    filename: detect_nodes.sh
    parameters:
# These are keyword which will substituted by jinja2
        uv_file: uv_10m_ERA5_2010.nc

stitch: 
    template: stitch_nodes.sh.j2
    target_volume: output
    filename: stitch_nodes.sh
    parameters:
# These are keyword which will substituted by jinja2
        stitch_file: ERA5_TC_tracks.txt
