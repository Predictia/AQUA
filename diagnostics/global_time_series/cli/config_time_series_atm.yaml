# This block can be overridden in the command line by using:
# --model, --exp, --source, --outputdir
model: "IFS"
exp: "tco2559-ng5-cycle3"
source: "lra-r100-monthly"
outputdir: "./"

timeseries:
  msl: # each variable will be a separate plot and can have custom kwargs
    plot_kw:
      color: "tab:blue"
      label: "MSL"
    plot_era5: True
    resample: "M"
    savefig: True
  2t: # each variable will be a separate plot and can have custom kwargs
    plot_kw:
      color: "tab:blue"
      label: "2t"
    plot_era5: True
    resample: "M"
    savefig: True

gregory:
  resample: "M"
  savefig: True