# Choice of interface
interface: AQUA
# Choice of model name. It is printed in the output table
model:
  name: AQUA

dirs:
  exp: '' #not necessary we use xarray dataset instead of files
  clm: /users/padavini/AQUA/diagnostics/ecmean/ECmean4/ecmean/climatology
  tab: /pfs/lustrep3/projappl/project_465000454/padavini/ecmean/output/YAML
  fig: /pfs/lustrep3/projappl/project_465000454/padavini/ecmean/output/PDF

global:
  atm_vars: [tas, psl, pr, evspsbl, pme, cll, clm, clh,
            pr_oce, pme_oce, pr_land, pme_land
            rsnt, rlnt, net_toa, rsns, rlns, hfss, hfls, net_sfc_nosn]
  oce_vars: [tos, sos]
  ice_vars: [siconc]
  tab_vars: []
  regions: ['Global', 'North Midlat', 'Tropical', 'South Midlat']
  seasons: ['ALL', 'DJF', 'JJA']

PI:
  2d_vars:
    field: [tas, pr, psl, tauu, tauv]
  3d_vars:
    field: [ta, ua, va, hus]
  oce_vars:
    field: [tos, sos]
  ice_vars:
    field: []
  regions: ['Global', 'North Midlat', 'Tropical', 'South Midlat']
  seasons: ['ALL', 'DJF', 'JJA']
  resolution: r360x180

