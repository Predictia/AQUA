# Choice of interface
interface: IFS+NEMO
# Choice of model name. It is printed in the output table
model:
   name: IFS+NEMO

dirs:
   exp: /work/bm1235/a270046/cycle2-sync/monthly_means/
   tab: /work/bb1153/b382076/ecmean/tables
   clm: /home/b/b382076/AQUA/diagnostics/ecmean/ECmean4/ecmean/climatology
   fig: /work/bb1153/b382076/ecmean/figures

global:
  atm_vars: [tas, psl, pr, pme, cll, clm, clh,
             pr_oce, pme_oce, pr_land, pme_land, 
             net_toa, rsnt, rlnt, rsntcs, rlntcs, swcf, lwcf, 
             rsns, rlns, hfss, hfls, net_sfc_nosn, net_sfc]
  oce_vars: [tos]
  ice_vars: [siconc_north, siconc_south]
  tab_vars: []

PI:
  2d_vars:
    field: [tas, pr, psl, tauu, tauv]
  3d_vars:
    field: [ta, ua, va, hus]
  oce_vars:
    field: [tos]
  ice_vars:
    field: [siconc]
  regions: ['Global', 'North Midlat', 'Tropical', 'South Midlat']
  seasons: ['ALL', 'DJF', 'JJA']
  resolution: r360x180

