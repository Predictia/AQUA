catalog:
  - ci

machine: pippo

paths:
  grids: 'pluto'

reader:
  catalog: '{{ configdir }}/catalogs/{{ catalog }}/catalog.yaml'
  machine: '{{ configdir }}/catalogs/{{ catalog }}/machine.yaml'
  fixer: '{{ configdir }}/fixes'
  regrid: '{{ configdir }}/grids'